{{ supervisor_path_log }}/*.log {
        daily
        missingok
        rotate 10
        compress
        delaycompress
        notifempty
        create 644 root root
        sharedscripts
        postrotate
                [ -f {{ supervisor_pidfile }} ] && kill -HUP `cat {{ supervisor_pidfile }}`
        endscript
}